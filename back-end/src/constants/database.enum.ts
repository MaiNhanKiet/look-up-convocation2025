export enum Role {
  STUDENT = 'STUDENT',
  EMPLOYEE = 'EMPLOYEE',
  ADMIN = 'ADMIN'
}

export enum Action {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
  VIEW = 'VIEW',
  LOGIN = 'LOGIN',
  LOGOUT = 'LOGOUT'
}

export enum Resource {
  USER = 'USER',
  EMPLOYEE = 'EMPLOYEE',
  VOTE = 'VOTE',
  RATING = 'RATING',
  AUDIT_LOG = 'AUDIT_LOG'
}
